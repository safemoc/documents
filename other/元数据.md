**元数据（Metadata）就是“描述数据的数据”。**

举个最简单的例子：

一张照片本身是数据；照片的拍摄时间、尺寸、文件格式、拍摄设备、地理位置，就是这张照片的元数据。

再比如数据库里有一张用户表：

```text
user
├── id
├── username
├── phone
└── created_at
```

用户表里面的具体记录属于业务数据：

```text
1, 张三, 138xxxx, 2026-07-30
```

而下面这些信息属于元数据：

- 表名叫 `user`
    
- 表里有几个字段
    
- 字段名称是什么
    
- `id` 是整数类型
    
- `phone` 最大长度是多少
    
- 哪个字段是主键
    
- 是否允许为空
    
- 表和其他表有什么关联
    

所以可以理解为：

> 业务数据回答“里面存了什么”，元数据回答“这些数据是什么、怎么组织、从哪里来、如何使用”。

常见的元数据主要包括三类。

**结构元数据**：描述数据的结构，例如数据库表结构、字段类型、JSON Schema、文件格式。

**业务元数据**：描述数据在业务上的含义，例如“注册用户数”的计算口径、“订单状态 2”代表已付款。

**技术元数据**：描述数据如何产生和流转，例如数据来源、更新时间、ETL 任务、负责人、数据存储位置。

拿你之前提到的 JS 埋点来说，一条事件数据可能是：

```json
{
  "event_name": "button_click",
  "user_id": 1001,
  "page": "/creation",
  "timestamp": 1785412800
}
```

它对应的元数据可能包括：

```text
event_name：事件名称，字符串，必填
user_id：用户唯一标识，整数
page：触发事件的页面路径
timestamp：客户端触发时间，Unix 时间戳
数据来源：Web 埋点 SDK
负责人：数据平台组
保留周期：180 天
```

这里有个容易混淆的地方：元数据通常也是以数据形式存储的。例如 MySQL 的 `information_schema` 中存储的表名、字段名、索引信息，本质上就是数据库的元数据。

一句话记忆：

> **数据是内容，元数据是关于内容的说明书。**